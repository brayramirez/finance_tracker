- if user_signed_in?
  .navbar.navbar-static-top
    .navbar-inner
      .container
        = link_to 'Finance Tracker', root_path, :class => 'brand pull-left'
        %ul.nav.pull-right
          %li.divider-vertical
          %li.navbar-text
            = "Welcome #{current_user.email}!"
          %li.divider-vertical
          %li
            = link_to 'Log out', destroy_user_session_path, :method => :delete